PORT=3000
PUBLIC_SITE_URL=https://area-maibab-public-site.onrender.com
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
PUBLISH_REPO=jmmeta6767/area-maibab-public-site
PUBLISH_BRANCH=main
GITHUB_PUBLISH_TOKEN=\n# Token must have Contents + Pull Requests write access only on the public-site repository.\n# Publisher creates area-seo/* branches and PRs; it never merges main.


# Optional: set only after mounting persistent storage for deployment history.
# DATA_DIR=/var/data/area-seo-ai
