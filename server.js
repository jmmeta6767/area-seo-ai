const http=require("http"),fs=require("fs"),path=require("path");const root=path.join(__dirname,"public"),port=process.env.PORT||3000;http.createServer((req,res)=>{let rel=req.url==="/"?"index.html":decodeURIComponent(req.url.split("?")[0].slice(1));let file=path.normalize(path.join(root,rel));if(!file.startsWith(root)){res.writeHead(403);return res.end("Forbidden")}fs.readFile(file,(e,d)=>{if(e){res.writeHead(404);return res.end("Not found")}res.setHeader("Content-Type",file.endsWith(".css")?"text/css":file.endsWith(".js")?"text/javascript":"text/html; charset=utf-8");res.end(d)})}).listen(port,()=>console.log("AREA SEO AI : "+port));